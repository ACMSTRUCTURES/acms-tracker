# ============ Folders ============
# Local dev scripts (not part of deployed PWA)
_tools/

# Legacy / reference snapshots — kept local for history
_archive/

# Excel source data + extracted JSONs (the canonical project data lives in OneDrive)
Input/

# OneDrive-shared data folder — synced through OneDrive itself, not git
ACMS Tracker/

# ============ Editor / OS junk ============
.DS_Store
Thumbs.db
.vscode/
.idea/

# ============ Sensitive data ============
# Real project data — lives in private OneDrive, never commit to public repo
Arena.json
*.acms-project.json
*.pyc
__pycache__/

# Build artefacts that the build_arena.py script may emit
*.pyc
__pycache__/
